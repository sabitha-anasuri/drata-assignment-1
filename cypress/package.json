{
  "scripts": {
  "cypress:run": "cypress run",
  "merge:reports": "mochawesome-merge cypress/reports/*.json > cypress/reports/mochareports.json",
  "generate:report": "marge cypress/reports/mochareports.json -f report -o cypress/reports"
  }
}